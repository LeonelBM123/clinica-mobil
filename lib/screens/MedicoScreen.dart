import 'package:flutter/material.dart';
class MedicoScreen extends StatelessWidget {
  const MedicoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Panel de Medico'),),
    );
  }
}